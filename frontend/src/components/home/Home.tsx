import React, { FunctionComponent } from "react";
import { Title } from "../title/Title";
import './Home.css'
export const Home: FunctionComponent = (prop) => {

    return (
        <React.Fragment>
            <Title>Home</Title>
        </React.Fragment>
    );
}